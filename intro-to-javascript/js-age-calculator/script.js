var userAge = prompt('Enter your age');

var daysInYear = 365.25;

var daysAlive = userAge * daysInYear;

/* Leap year every four years */

alert('You have been alive for an estimated' + daysAlive + ' days!');